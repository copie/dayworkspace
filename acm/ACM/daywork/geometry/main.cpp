#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    int n,m;
    int k;
    cin>>k;
    while(k--)
    {    cin>>n>>m;
    cout<<sqrt(n*n+n*n)*sqrt(m*m+m*m)<<endl;

    }

}
